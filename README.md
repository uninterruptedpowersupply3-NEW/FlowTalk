# FlowTalk
This is an **experimental research prototype** multimodal model that combines: - **Flow-matching image generation** in VAE latent space - **Autoregressive text generation** (next-token prediction) It is **not** a production-quality text-to-image model. Prompt adherence is inconsistent and strongly depends on matching the **training prompt format**.
